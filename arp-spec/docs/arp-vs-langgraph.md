# ARP vs. LangGraph

LangGraph is oriented toward designing stateful agent workflows. ARP is oriented toward defining a standard responsibility layer that can be applied to those workflows.

A LangGraph-based implementation can use ARP declarations as policy inputs, but ARP does not prescribe graph execution, node semantics, or orchestration patterns.
